name: learn-github-actions-2
on: [pull_request]
jobs:
  check-something:∫
    runs-on: [ubuntu-latest, self-hosted]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
      - run: npm install -g bats
      - run: bats -v